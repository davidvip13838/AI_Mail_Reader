{
  "name": "ai-mail-reader",
  "version": "1.0.0",
  "description": "AI Mail Reader - Gmail summarization with audio generation",
  "main": "backend/server.js",
  "scripts": {
    "start": "cd backend && npm start"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}

